class Solution {
    public boolean search(int[] nums, int target) {
        int n=nums.length;
        boolean val=false;
        for(int i=0;i<n;i++){
            if(nums[i]==target){
                val=true;
                break;
            }
        }
        return val;
    }
}
